---
title: "Agents"
description: "AI workers that run autonomous tasks and interactive sessions on isolated microVMs."
icon: "robot"
---

ZWRM Agents are AI workers — Claude Code, OpenAI Codex, or the native **zwrm runtime** — that run in isolated Firecracker microVMs. An **agent is pure configuration** — instructions, default model, effort, VM size, connectors, skills, and budgets. Execution state lives in **workspaces**: a persistent volume plus a session-continuity key. VMs are **ephemeral**: one boots when a run or chat starts and is torn down when it finishes; the workspace volume (files, repos, conversation transcript) is what carries everything forward.

Agents have full access to the ZWRM platform — the `zwrm` CLI is pre-installed and authenticated inside every VM. Your agent can build, deploy, scale, and manage applications directly.

## Quick start

```bash
# Boot into an interactive SSH workspace
zwrm agent claude

# Dispatch an autonomous run (returns immediately)
zwrm agent run --agent default --prompt "Summarize the open PRs in acme/api"
zwrm agent run status <run-id>

# Continue that exact conversation later
zwrm agent run continue <run-id> --prompt "Now address the review feedback"
```

On first use, the CLI prompts for required credentials (Claude/Codex auth, GitHub token, git config). They are stored as agent secrets and injected into every VM at boot; `gh` is authenticated automatically.

## The model

| Concept | What it is |
|---------|------------|
| **Agent** | A named definition: instructions, defaults (model, effort, size), connectors, skills, budgets. Never owns a VM. |
| **Workspace** | A volume + a session key. Same key ⇒ same conversation and files; no key ⇒ a fresh anonymous workspace (72h TTL). |
| **Run** | One autonomous task: a prompt executed as a headless session in a fresh VM on the chosen workspace. |
| **Session** | One live conversation (a run's headless session, or a dashboard chat). One live session per workspace. |

There is no built-in repo concept: a repo is whatever the prompt (or a skill) clones onto the workspace volume. `gh` is pre-authenticated, so "clone acme/api and open a PR" just works — and the clone persists on the volume for the next run with the same session key.

## Agent types

The type is set once, at creation (`zwrm agent <type>`), and picks the harness the agent runs on:

| Type | Harness | Billing |
|------|---------|---------|
| `claude` | Claude Code | Your org's Anthropic credentials |
| `codex` | OpenAI Codex | Your org's OpenAI credentials (API key or ChatGPT subscription) |
| `pi` | The **zwrm runtime** (shown as **zwrm** in the dashboard; EU-hosted open models) | Prepaid [credits](/billing/credits) |

Each harness exposes its own model catalog and reasoning-effort ladder — see [Models](/agents/models). claude and codex run on credentials you bring; the zwrm runtime is metered against prepaid credits with no key to manage.

Agent names are **unique per organization, across types**: `default` can name a claude agent *or* a codex agent in an org, not both. Connecting with the wrong type for an existing name is rejected. Connecting to a name that doesn't exist creates it.

## Configuring an agent

Everything an agent *is* can be changed with a single command — only the flags you pass change:

```bash
zwrm agent update my-project --model opus --effort high --size performance-2x
```

| Flag | Description |
|------|-------------|
| `--size` | Default VM size preset (e.g. `performance-2x`) |
| `--model` | Default model — harness-specific (`opus`/`sonnet` for claude, catalog aliases for codex and the zwrm runtime). See [Models](/agents/models) |
| `--effort` | Default reasoning effort: `low`, `medium`, `high`, `xhigh`, `max` |
| `--instructions` | Per-agent system-prompt append |
| `--workspace-ttl` | Keyed workspace retention (e.g. `720h`; `0` = never expire) |
| `--daily-budget` | Daily spend cap in USD (`0` = unlimited) |
| `--max-runs-per-day` | Daily run-count cap (`0` = unlimited) |
| `--allowed-scopes` | [Capabilities](/agents/capabilities) the agent's platform token carries |

## What agents can do

From inside any agent VM, the agent (or you, over SSH) can:

- **Deploy apps** — `zwrm deploy` to build and ship directly from the agent
- **Manage secrets** — `zwrm secrets set` to configure environment variables
- **Create databases** — `zwrm postgres create` to spin up Postgres instances
- **Scale services** — `zwrm scale` to adjust replicas
- **View logs** — `zwrm logs -f` to stream application output
- **SSH into apps** — `zwrm ssh` to debug running VMs

## Where to go next

<CardGroup cols={2}>
  <Card title="Runs" icon="play" href="/agents/runs">
    Dispatch autonomous tasks and iterate on them.
  </Card>
  <Card title="Workspaces" icon="folder-tree" href="/agents/workspaces">
    Session continuity, files, and retention.
  </Card>
  <Card title="Models" icon="microchip" href="/agents/models">
    Harnesses, model catalogs, and how each is billed.
  </Card>
  <Card title="Waiting & outputs" icon="hourglass-half" href="/agents/messaging">
    How a run waits, delivers results, and reaches a human.
  </Card>
  <Card title="Interactive sessions" icon="terminal" href="/agents/interactive">
    SSH sessions, dashboard chat, and managing agents.
  </Card>
  <Card title="OpenAI-compatible API" icon="message-bot" href="/agents/openai-api">
    Chat with any agent from any OpenAI client.
  </Card>
  <Card title="Connectors" icon="plug" href="/context/connectors">
    Attach MCP tools from the gateway — credentials never enter the VM.
  </Card>
  <Card title="Skills" icon="wand-magic-sparkles" href="/context/skills">
    Reusable instruction bundles synced to the workspace.
  </Card>
  <Card title="Memory" icon="brain" href="/context/memory">
    Durable knowledge that survives sessions and workspaces.
  </Card>
  <Card title="Capabilities" icon="shield-halved" href="/agents/capabilities">
    Scope what an agent may do on the platform.
  </Card>
  <Card title="Budgets" icon="coins" href="/agents/budgets">
    Cap spend and run counts per day.
  </Card>
  <Card title="Automations" icon="bolt" href="/automations/triggers">
    Trigger runs from external events and schedules.
  </Card>
</CardGroup>
